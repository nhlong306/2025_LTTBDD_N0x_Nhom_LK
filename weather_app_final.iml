<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>weather_app_final</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <!-- Thêm CSS để mô phỏng khung điện thoại -->
    <style>
        /* Đảm bảo toàn bộ body chiếm toàn bộ viewport */
        body {
            display: flex;
            justify-content: center; /* Căn giữa theo chiều ngang */
            align-items: center;    /* Căn giữa theo chiều dọc */
            min-height: 100vh;
            margin: 0;
            background-color: #f0f0f0; /* Màu nền ngoài khung điện thoại */
        }

        /* Định nghĩa khung mô phỏng điện thoại */
        #mobile-frame {
            /* Kích thước tiêu chuẩn của điện thoại */
            width: 400px; 
            height: 800px;
            
            /* CSS trang trí khung (tùy chọn) */
            background-color: white;
            border-radius: 30px;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
            overflow: hidden; /* Giới hạn nội dung bên trong */

            /* Đảm bảo ứng dụng Flutter nằm trong khung này */
            position: relative; 
        }

        /* Áp dụng các quy tắc cho canvas của Flutter (mặc định là #fluter_container) */
        #flutter_container {
            width: 100%;
            height: 100%;
            margin: 0;
        }
    </style>
    
    <!--
      The web platform code is provided in this file, so that it can be included in your
      html before main.dart.js is loaded. It provides the flutter web entry point,
      and initializes the flutter view.
    -->
    <script src="flutter.js" defer></script>
</head>
<body>
    <!-- Thẻ div chứa khung mô phỏng điện thoại -->
    <div id="mobile-frame">
        <!-- Thẻ div mặc định của Flutter, nơi ứng dụng của bạn được render -->
        <div id="flutter_container"></div>
    </div>
    
    <!-- Đoạn JavaScript khởi tạo ứng dụng đã được đơn giản hóa -->
    <script>
        window.addEventListener('load', function(ev) {
            // Khởi tạo engine Flutter
            _flutter.loader.loadEntrypoint({
                entrypointUrl: "main.dart.js",
                onEntrypointLoaded: async function(engineInitializer) {
                    let appRunner = await engineInitializer.initializeEngine();
                    await appRunner.runApp();
                }
            });
        });
    </script>
</body>
</html>